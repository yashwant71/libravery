{
    "name": "librarvery-monorepo",
    "version": "1.0.0",
    "scripts": {
        "dev:backend": "cd backend && npm install && node --watch src/server.js",
        "dev:frontend": "cd frontend && npm install && npm run dev",
        "install:all": "cd backend && npm install && cd ../frontend && npm install"
    },
    "devDependencies": {
        "@types/node": "^24.3.0"
    }
}